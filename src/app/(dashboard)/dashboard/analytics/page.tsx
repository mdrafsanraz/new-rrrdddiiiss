import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export const metadata = { title: "Analytics" };

export default function AnalyticsPage() {
  return (
    <SectionPlaceholder
      title="Analytics"
      body="Stream analytics unlock on Pro after releases go live. Charts will use our analytics layer — never exposed LabelGrid credentials."
    />
  );
}
