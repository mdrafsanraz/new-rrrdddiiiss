import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export const metadata = { title: "Support" };

export default function SupportPage() {
  return (
    <SectionPlaceholder
      title="Support"
      body="RDISTRO is your first line of support. Email hello@rdistro.com for catalog, billing, or release questions — you never need to contact the distributor directly."
    />
  );
}
